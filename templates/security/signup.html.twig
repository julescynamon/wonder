{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
    <div class="block">

        <h2 class="pb-20">Inscription</h2>

        {{ form_start(form) }}
        {{ form_rest(form) }}
        <button type="submit" class="btn btn-primary">S'inscrire</button>
        <p class="mt-10">Vous avez déjà un compte ? Connectez vous <a class="redirect" href="{{ path('login') }}">içi</a></p>
        {{ form_end(form) }}

    </div>
{% endblock %}
